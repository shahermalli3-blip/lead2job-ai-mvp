{
  "name": "lead2job-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Native mobile packaging for the Lead2Job customer app.",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:assets": "capacitor-assets generate",
    "mobile:sync": "npm run mobile:prepare && cap sync",
    "mobile:android": "npm run mobile:sync && cap open android",
    "mobile:ios": "npm run mobile:sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/assets": "3.0.5",
    "@capacitor/cli": "8.5.0",
    "typescript": "5.9.2"
  }
}
